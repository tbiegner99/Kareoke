# Test Environment Variables
TEST_API_BASE_URL=http://localhost:3000

# Database for tests (if different from dev)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=kareoke_test
DB_USER=postgres
DB_PASSWORD=password

# Test specific settings
NODE_ENV=test
